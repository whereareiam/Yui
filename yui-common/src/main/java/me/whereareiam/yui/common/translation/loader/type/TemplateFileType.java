package me.whereareiam.yui.common.translation.loader.type;

/**
 * Handler for TEMPLATE type files.
 * Template files are loaded using the same logic as LOCALE files,
 * so this type simply delegates to LocaleFileType.
 */
public class TemplateFileType extends LocaleFileType {
    // Templates use the same flat structure as locale files
}
